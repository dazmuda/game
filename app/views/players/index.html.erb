<h1>Click Quest - Player Scores</h1>

<% @players.each do |player|%>

	<li><%= link_to "#{player.name} - #{player.score} mobs defeated", player_path(player) %></li>
	
<% end %>

<br><%= link_to "Create a new character", new_player_path %>